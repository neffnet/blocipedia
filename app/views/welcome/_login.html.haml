= simple_form_for(:user, :url => session_path(:user)) do |f|
  = f.text_field :name, autofocus: true, placeholder: 'username'
  = f.password_field :password, placeholder: 'password'
  = f.submit 'Sign In'
  = f.check_box :remember_me
  = f.label :stay_signed_in?
